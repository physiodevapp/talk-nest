<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TalkNest App</title>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="icon" href="../images/talkNest_logo_no_bg.png">
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="m-0 p-0 overflow-hidden bg-[url('/images/watercolor_background.jpg')] flex items-center justify-center min-h-screen">

  <!-- Main Container -->
  <main class="w-full h-screen bg-white sm:w-[320px] sm:h-[700px] sm:rounded-[40px] sm:border-8 sm:border-gray-900 sm:shadow-lg sm:drop-shadow-[0_35px_25px_rgba(0,0,0,0.25)] relative">

    <!-- Notch -->
    <div class="hidden sm:block absolute top-0 left-1/2 -translate-x-1/2 w-28 h-4 bg-black rounded-b-[20px]"></div>

    <!-- Title Section -->
    <article class="p-6 sm:p-8 flex flex-col justify-center h-full bg-white rounded-[40px]">
      <h2 class="text-center text-4xl sm:text-3xl font-bold text-gray-800 mb-8 sm:mb-6">
        <% if (formType !== 'forgot') { %><img class="inline-block w-[1.4em]" src="../images/talkNest_logo.png"/> TalkNest <% } %>
        <% if (formType === 'forgot') { %> Forgot Password <% } %>
      </h2>

      <!-- Form -->
      <% if (formType === 'login') { %>
        <form action="/login" method="POST" class="space-y-4">
          <input type="text" name="username" placeholder="Username"
            class="w-full px-5 py-4 text-lg text-gray-700 bg-gray-100 rounded-full focus:outline-none focus:ring-2 focus:ring-blue-500 sm:text-base sm:py-3 sm:px-4" />
          <input type="password" name="password" placeholder="Password"
            class="w-full px-5 py-4 text-lg text-gray-700 bg-gray-100 rounded-full focus:outline-none focus:ring-2 focus:ring-blue-500 sm:text-base sm:py-3 sm:px-4" />
          <button type="submit"
            class="w-full bg-[#D6244F] text-white text-lg py-4 rounded-full hover:bg-[#B71C3B] transition font-semibold sm:text-base sm:py-3">
            Login
          </button>
          <% if (typeof error !== 'undefined') { %>
            <p class="text-red-600 text-center"><%= error %></p>
          <% } %>
        </form>
      <% } %>

      <% if (formType === 'register') { %>
        <form action="/register" method="POST" class="space-y-4">
          <input type="text" name="username" placeholder="Username"
            class="w-full px-5 py-4 text-lg text-gray-700 bg-gray-100 rounded-full focus:outline-none focus:ring-2 focus:ring-blue-500 sm:text-base sm:py-3 sm:px-4" />
          <input type="email" name="email" placeholder="Email"
            class="w-full px-5 py-4 text-lg text-gray-700 bg-gray-100 rounded-full focus:outline-none focus:ring-2 focus:ring-blue-500 sm:text-base sm:py-3 sm:px-4" />
          <input type="password" name="password" placeholder="Password"
            class="w-full px-5 py-4 text-lg text-gray-700 bg-gray-100 rounded-full focus:outline-none focus:ring-2 focus:ring-blue-500 sm:text-base sm:py-3 sm:px-4" />
          <button type="submit"
            class="w-full bg-[#D6244F] text-white text-lg py-4 rounded-full hover:bg-[#B71C3B] transition font-semibold sm:text-base sm:py-3">
            Register
          </button>
        </form>
      <% } %>

      <% if (formType === 'forgot') { %>
        <form action="/forgot" method="POST" class="space-y-4">
          <input type="email" name="email" placeholder="Enter your email"
            class="w-full px-4 py-3 text-gray-700 bg-gray-100 rounded-full focus:outline-none focus:ring-2 focus:ring-blue-500" />
          <button type="submit"
            class="w-full bg-blue-600 text-white py-3 rounded-full hover:bg-blue-700 transition font-semibold">
            Send Reset Link
          </button>
        </form>
      <% } %>

      <!-- Divider -->
      <% if (formType !== 'forgot') { %>
        <div class="flex items-center my-6">
          <hr class="flex-grow border-gray-300">
          <span class="mx-4 text-gray-600 text-lg sm:text-base">or</span>
          <hr class="flex-grow border-gray-300">
        </div>

        <!-- Social Login Buttons -->
        <form action="/auth/google" method="GET" class="space-y-4">
          <button type="submit" class="w-full bg-white border-gray-300 text-[#5F6368] text-lg py-3 rounded-full hover:shadow-md transition font-semibold flex items-center justify-center sm:text-base sm:py-3">
            Google <img src="/images/chrome.png" alt="Google" class="w-6 ml-2">
          </button>
        </form>
      <% } %>

      <!-- Links -->
      <% if (formType === 'login') { %>
        <p class="text-center mt-4 text-gray-600 text-lg sm:text-base">
          Don't have an account? <a href="/access?formType=register" class="block text-blue-600 font-semibold">Register here</a>
        </p>
      <% } else if (formType === 'register') { %>
        <p class="text-center mt-4 text-gray-600 text-lg sm:text-base">
          Already have an account? <a href="/access?formType=login" class="block text-blue-600 font-semibold">Login here</a>
        </p>
      <% } %>
    </article>
  </main>

</body>
</html>
